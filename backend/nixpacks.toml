[build]
node-version = "18.20.8"
npm-version = "latest"

[build.install]
run = ["npm ci --legacy-peer-deps"]

[build.setup]
run = ["echo 'Building CineHive Backend...'"]

[start]
run = "npm start"

[envs]
NODE_ENV = "production"
